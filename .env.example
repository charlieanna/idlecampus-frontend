# API Configuration
# Base URL for the API (without trailing slash)
# In development, this will use the Vite proxy configured in vite.config.ts
VITE_API_URL=/api/v1

# For production, set this to your actual API URL
# VITE_API_URL=https://api.idlecampus.com/api/v1

# Progressive Flow Backend Configuration
# URL for the Progressive Flow backend API
VITE_PROGRESSIVE_FLOW_API_URL=http://localhost:3001

# Enable/disable backend mode for Progressive Flow
# Set to 'true' to use PostgreSQL backend, 'false' for localStorage
VITE_PROGRESSIVE_FLOW_USE_BACKEND=false
